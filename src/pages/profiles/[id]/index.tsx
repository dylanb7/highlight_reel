import { type NextPage } from "next";
import { ProfilePage } from "~/components/profileComponents/profile-page";

const ProfileById: NextPage = () => {
  return <ProfilePage />;
};

export default ProfileById;
